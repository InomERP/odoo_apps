<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_create_session_wizard_form" model="ir.ui.view">
        <field name="name">stock.inventory.count.session.wizard.form</field>
        <field name="model">stock.inventory.count.session.wizard</field>
        <field name="arch" type="xml">
            <form string="Create Session">
                <field name="count_id" invisible="1"/>
                <group>
                    <field name="user_ids" widget="many2many_tags"
                           options="{'no_create': True}"/>
                </group>
                <group string="Products to Count">
                    <field name="product_ids" nolabel="1" readonly="1"
                           widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_create_sessions" string="Create"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_create_session_wizard" model="ir.actions.act_window">
        <field name="name">Create Session</field>
        <field name="res_model">stock.inventory.count.session.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
