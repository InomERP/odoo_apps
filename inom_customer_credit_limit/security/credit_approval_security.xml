<odoo>
    <!-- ============================================================== -->
    <!-- Credit Limit Approver group                                      -->
    <!-- Note: in Odoo 17 res.groups still uses category_id; the group is -->
    <!-- left uncategorized intentionally (categorization is cosmetic).   -->
    <!-- ============================================================== -->
    <record id="group_credit_approval_manager" model="res.groups">
        <field name="name">Credit Limit Approver</field>
        <field name="comment">Users who can approve or reject customer credit limit approval requests.</field>
    </record>

    <!-- Admin (technical) is a Credit Limit Approver by default -->
    <record id="base.user_admin" model="res.users">
        <field name="groups_id" eval="[(4, ref('group_credit_approval_manager'))]"/>
    </record>

    <!-- Every System Administrator is also a Credit Limit Approver.
         Purely additive: grants the approver capability to all admin users
         (e.g. "YourCompany" admin) without removing any access or altering
         any sale workflow. -->
    <record id="base.group_system" model="res.groups">
        <field name="implied_ids" eval="[(4, ref('group_credit_approval_manager'))]"/>
    </record>

    <!-- ============================================================== -->
    <!-- Record rules                                                     -->
    <!-- ============================================================== -->
    <record id="rule_credit_approval_salesman_own" model="ir.rule">
        <field name="name">Credit Approval: Salesperson own requests</field>
        <field name="model_id" ref="model_credit_approval_request"/>
        <field name="domain_force">['|', ('requested_by', '=', user.id), ('sale_order_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_credit_approval_manager_all" model="ir.rule">
        <field name="name">Credit Approval: Managers see all</field>
        <field name="model_id" ref="model_credit_approval_request"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_manager')), (4, ref('group_credit_approval_manager'))]"/>
    </record>
</odoo>