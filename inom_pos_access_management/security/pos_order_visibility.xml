<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <data>
        <record id="rule_pos_order_salesperson_visibility" model="ir.rule">
            <field name="name">POS Order: salesperson sees only own orders</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="domain_force">[
    '|',
        '|',
            '|',
                ('user_id', '=', user.id),
                ('employee_id.user_id', '=', user.id),
            (('id', '!=', 0) if (user.has_group('point_of_sale.group_pos_manager') or user.has_group('base.group_system')) else ('id', '=', 0)),
        (('id', '!=', 0) if not user.env['pos.access.rights'].sudo().search_count([('user_id', '=', user.id), ('active', '=', True), ('restrict_salesperson_orders', '=', True)]) else ('id', '=', 0)),
]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo> -->