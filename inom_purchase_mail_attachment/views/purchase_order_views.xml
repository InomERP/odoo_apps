<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="purchase_order_form_inherit_purchase_mail" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.purchase.mail</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button type="object"
                        name="action_view_purchase_mail_documents"
                        class="oe_stat_button"
                        icon="fa-paperclip"
                        invisible="purchase_document_count == 0">
                    <field name="purchase_document_count"
                           widget="statinfo"
                           string="Email Docs"/>
                </button>
            </div>
        </field>
    </record>
</odoo>