<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_univ_fee_installment_plan_list" model="ir.ui.view">
        <field name="name">univ.fee.installment.plan.list</field>
        <field name="model">univ.fee.installment.plan</field>
        <field name="arch" type="xml">
            <tree string="Installment Plans"
                  decoration-danger="state == 'default'"
                  decoration-success="state == 'done'">
                <field name="name"/>
                <field name="student_id"/>
                <field name="invoice_id"/>
                <field name="frequency"/>
                <field name="count"/>
                <field name="amount_total"/>
                <field name="state" widget="badge"
                       decoration-danger="state == 'default'"
                       decoration-success="state == 'done'"/>
                <field name="currency_id" column_invisible="1"/>
            </tree>
        </field>
    </record>

    <record id="view_univ_fee_installment_plan_form" model="ir.ui.view">
        <field name="name">univ.fee.installment.plan.form</field>
        <field name="model">univ.fee.installment.plan</field>
        <field name="arch" type="xml">
            <form string="Installment Plan">
                <header>
                    <button name="generate_schedule" type="object"
                            string="Generate Schedule" invisible="state != 'draft'"/>
                    <button name="action_confirm" type="object" string="Confirm"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_reset_to_draft" type="object"
                            string="Reset to Draft"
                            invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="invoice_id"/>
                            <field name="student_id"/>
                        </group>
                        <group>
                            <field name="frequency"/>
                            <field name="count"/>
                            <field name="start_date"/>
                            <field name="amount_total"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <field name="line_ids">
                        <tree editable="bottom">
                            <field name="sequence"/>
                            <field name="due_date"/>
                            <field name="amount" sum="Total"/>
                            <field name="state" widget="badge"
                                   decoration-success="state == 'paid'"
                                   decoration-danger="state == 'overdue'"/>
                            <field name="paid_date"/>
                            <field name="currency_id" column_invisible="1"/>
                            <button name="action_mark_paid" type="object"
                                    string="Mark Paid" icon="fa-check"
                                    invisible="state == 'paid'"/>
                        </tree>
                    </field>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_univ_fee_installment_plan" model="ir.actions.act_window">
        <field name="name">Installment Plans</field>
        <field name="res_model">univ.fee.installment.plan</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
